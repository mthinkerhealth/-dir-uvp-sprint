# Files excluded from the Cloudflare static-asset upload (.gitignore-style syntax).
# node_modules is installed during the build and includes binaries (e.g. workerd) that
# exceed the 25 MiB per-asset limit — it must never be served as a static asset.
node_modules
.git
.wrangler
.dev.vars
.dev.vars.example

# Not part of the public site
test
fixtures
package.json
package-lock.json
