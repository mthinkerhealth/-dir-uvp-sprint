# Cloudflare Pages headers — https://developers.cloudflare.com/pages/configuration/headers/

# Site-wide security headers (apply to every path).
/*
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=()
  Content-Security-Policy: default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'none'; form-action 'self'; img-src 'self' data: https:; font-src 'self' https://fonts.gstatic.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; script-src 'self' 'unsafe-inline' https://www.googletagmanager.com https://www.google-analytics.com https://*.google-analytics.com https://challenges.cloudflare.com; connect-src 'self' https://www.googletagmanager.com https://www.google-analytics.com https://*.google-analytics.com https://*.analytics.google.com https://challenges.cloudflare.com; frame-src https://challenges.cloudflare.com; upgrade-insecure-requests

# app.js has a stable (non-hashed) filename, so a long TTL means deploys don't reach
# returning visitors for hours. Short TTL + revalidation: fixes propagate within minutes,
# unchanged file still returns a cheap 304 via ETag.
/app.js
  Cache-Control: public, max-age=600, must-revalidate
  X-Content-Type-Options: nosniff

# Static document pages: always revalidate so content edits go live immediately.
/*.html
  Cache-Control: public, max-age=0, must-revalidate

# Blog sections are directory-index pages, so the request path (/case-studies/foo/)
# never ends in .html and the /*.html rule above cannot match them. Same intent:
# always revalidate so content edits go live immediately.
/case-studies/*
  Cache-Control: public, max-age=0, must-revalidate

/how-it-works/*
  Cache-Control: public, max-age=0, must-revalidate

# Shared blog stylesheet has a stable (non-hashed) filename, same tradeoff as app.js:
# short TTL + revalidation so a design fix reaches returning visitors in minutes,
# unchanged file still returns a cheap 304 via ETag.
/assets/post.css
  Cache-Control: public, max-age=600, must-revalidate
  X-Content-Type-Options: nosniff
