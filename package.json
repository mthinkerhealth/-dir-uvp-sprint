{
  "name": "dir-uvp-sprint",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DIR UVP Sprint — static site + Cloudflare Pages Functions. No build step; this manifest exists only to run the test suite.",
  "scripts": {
    "test": "node --test"
  }
}
